Perfecto, entiendo exactamente lo que necesitas. Vas a reemplazar completamente el modal de edición actual del dashboard para que use exactamente la misma lógica y componentes que la sección de gestión de campañas. Esto significa:

Mismo pop-up: Reutilizar el formulario completo de campanas-management.tsx
Misma lógica: useForm + zodResolver + FormField components
Misma tabla: Todo se guarda en campanas_comerciales (no confusión)
Individual: Cada botón "Editar" abre el form pre-cargado con los datos de esa campaña específica
Voy a implementar esto siguiendo exactamente el patrón existente:
dame las instrucciones que vas a seguir
4 minutes ago
📋 Instrucciones Detalladas que Voy a Seguir
1. Importar Dependencias Necesarias
Agregar react-hook-form, zodResolver
Importar todos los Form components de shadcn
Importar esquemas y tipos de @shared/schema
Importar constantes MARCAS_DISPONIBLES y ZONAS_DISPONIBLES
2. Configurar useForm (Idéntico a campanas-management)
const form = useForm<Omit<InsertCampanaComercial, 'fechaFin' | 'numeroCampana'>>({
  resolver: zodResolver(insertCampanaComercialSchema.omit({ fechaFin: true, numeroCampana: true })),
  defaultValues: {
    clienteId: 0,
    cantidadDatosSolicitados: 0,
    marca: "",
    zona: "",
    fechaCampana: "",
    pedidosPorDia: 0,
    facturacionBruta: 0,
  },
});
3. Crear updateMutation (Copiado Exacto)
Usar la misma función que en campanas-management.tsx
Mismas invalidaciones de queries
Mismos mensajes de toast
Misma lógica de éxito/error
4. Query para Clientes
Agregar useQuery para obtener lista de clientes: ['/api/clientes']
Necesario para el select de cliente en el formulario
5. Modificar handleOpenEditForm
Buscar campaña por numeroCampana en /api/campanas-comerciales
Usar form.reset() para cargar datos (exacto como campanas-management)
Pre-llenar todos los campos del formulario
6. Reemplazar Modal Completo
Eliminar los inputs simples actuales
Instalar estructura <Form> con FormField components
Copiar exactamente cada campo del formulario de campanas-management.tsx:
Cliente (Select)
Cantidad de Datos (Input numérico)
Fecha de Inicio (Input date)
Marca (Select con opciones)
Zona (Select con provincias)
Localización (Input texto)
Pedidos por Día (Input numérico)
Facturación Bruta (Input decimal)
7. Función onSubmit
Usar form.handleSubmit()
Llamar updateMutation.mutate() con datos del form
Misma lógica que campanas-management
8. Validación y Testing
Verificar que cada campaña abre con sus datos específicos
Confirmar que los cambios se guardan en tabla campanas_comerciales
Verificar actualización automática del dashboard
🎯 Resultado Final Esperado
Cuando hagas clic en cualquier botón "Editar" del dashboard:

✅ Abre el mismo modal que gestión de campañas
✅ Pre-cargado con datos específicos de esa campaña
✅ Validación completa con Zod schema
✅ Selects interactivos para marca/zona/cliente
✅ Guarda en campanas_comerciales (sin confusión de tablas)
✅ Actualización inmediata del dashboard tras guardar

¿Procedo con la implementación siguiendo exactamente estas instrucciones?